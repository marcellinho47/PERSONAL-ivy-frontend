import 'package:flutter/material.dart';

class ColorPallete {
  static const Color backgroudSystem = Color.fromRGBO(165, 165, 165, 1);
  static const Color primaryColor = Color.fromRGBO(0, 53, 102, 1);
  static const Color onPrimaryColor = Colors.white12;
}
